'use client';
import {useState} from 'react';
export default function PhoneLookup(){const [phone,setPhone]=useState('');const [data,setData]=useState<any>(null);const [loading,setLoading]=useState(false);async function run(){setLoading(true);const r=await fetch('/api/v1/phone/lookup',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({phone})});setData(await r.json());setLoading(false)}return <><div className="search"><input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="+55 (71) 99999-9999"/><button onClick={run}>{loading?'CONSULTANDO...':'IDENTIFICAR'}</button></div>{data&&<div className="card" style={{maxWidth:680,margin:'24px auto',textAlign:'left'}}><pre style={{whiteSpace:'pre-wrap',margin:0}}>{JSON.stringify(data,null,2)}</pre></div>}</>}
